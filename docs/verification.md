# 测试与验证

状态：active

验证日期：2026-09-07（下方保留历史验证记录）。

## Agent 并发与提问体验移植

2026-09-07：Agent 异步写路由的同步数据库阶段移入工作线程；附件读取有超时与并发准入，PDF 解析不持有数据库会话，完成后重新校验文件状态。提问响应按 Run、Question 顺序加锁并刷新已缓存状态。

来源上下文只作为首次用户历史注入，覆盖编辑、重新生成、压缩与提问重启恢复；`0003_source_history` 迁移覆盖已有消息、运行检查点和提问恢复载荷，升级、降级及再次升级均在隔离数据库中验证。已有实例需停服后执行迁移，本次未升级用户运行数据库。

真实本机 PostgreSQL / Redis 全量测试 109 项通过，使用隔离 schema 和 Redis 命名空间。默认后端回归 105 项通过、4 项 PostgreSQL 专用测试跳过；前端新增结构化错误、本地化切换与长选项测试。`make check` 和 `make build` 通过。

合成浏览器会话验证桌面选项省略与全文提示、390px 和 320px 手机完整换行及列表滚动，无横向溢出；深色模式和语言切换保留原始选项。提交选项后运行继续，刷新页面仍恢复最终回答。上传错误切换语言不重复上传或删除文件。

## README 循环演示

README 使用 `/rendering` 的完整英文合成流程录制，覆盖思考、工具调用、流式正文与过程自动收起。浅色与深色 GIF 随 GitHub 主题切换，点击进入在线 `/rendering` 交互演示。

录制脚本复用页面现有播放按钮与时间轴；仅在独立录制浏览器中隐藏导航和调试按钮，不修改应用代码，不调用模型或创建会话。成片裁去加载过程及录制同步标记，只保留演示区。旧首页截图保留为历史验证证据。

本机准备 `ffmpeg`（含 `ffprobe`）、Node.js / npx，并先执行 `make dev`，然后运行：

```sh
uv run --python 3.12 python scripts/record_readme_demo.py
```

脚本使用固定版本 `agent-browser` 录制，输出 `screenshots/rendering-light.gif`、`screenshots/rendering-dark.gif`：960 × 560、25 帧/秒、完整流程后停留 3 秒、无限循环。自动检查英文、主题、内容完整性、裁切与循环标记；更新后仍需目视检查关键帧和 README 浅色/深色展示。

本次成片均为 18.8 秒、470 帧；浅色约 0.44 MiB，深色约 0.51 MiB。已核对英文关键帧、表格边框和背景色阶，以及 README 图片主题切换和跳转地址。脚本 Ruff / 编译检查、`make check`、`make build` 通过。

## 品牌、侧栏图标与署名

侧栏标题左侧使用 24px 独立 SVG 图标，图形和文字中心偏差为 0px；CSS mask 随深浅主题切换。Vite 内联 SVG 的 URL 使用引号，避免数据 URL 内的属性引号使 mask 声明失效。

应用通用图标统一使用 `lucide-react`，覆盖左侧导航、聊天列表、折叠按钮、聊天工具栏、历史搜索弹窗与 Agent 面板；主导航和工具栏图标尺寸为 16px、描边为 1.75。`Made by xiaotongyu.com` 居中放在左侧菜单底部、导航滚动区之外，所有页面复用；菜单关闭时一同隐藏。署名保留鼠标和 Enter 激活的 canvas-confetti 效果，原点按按钮实时位置计算，每次分两组发射 130 个粒子；系统减少动态效果时不创建烟花画布。

1440px 桌面、200px 最小侧栏与 390px 手机抽屉中，署名相对侧栏中心偏差为 0.5px、距底部 12px，无横向溢出；鼠标、Enter 与手机点击的烟花原点偏差均为 0px。折叠时署名不可见且不可交互，重新展开后恢复；深浅主题和中英文切换正常并保留输入草稿。

浏览器使用空白会话与合成能力响应验证，不调用模型、不写入用户会话。浅色首页、深色首页与烟花截图分别见 [浅色首页](screenshots/home.png)、[深色首页](screenshots/home-dark.png)、[署名烟花](screenshots/credit-fireworks.png)。旧截图保留为对应历史验证的证据。

侧栏调整后 `make check`、`make build` 通过：前端 99 项通过，后端默认回归 88 项通过、2 项 PostgreSQL 专用测试跳过。首次开源发布的真实 PostgreSQL / Redis 回归及 README 本地链接检查通过；发布文件检查排除 `.env`、运行时数据、依赖目录和构建产物。

历史搜索、会话选中勾号与 Agent 面板的图标在浏览器合成会话中验证通过，打开和关闭操作正常，无脚本错误或 API 写请求。源码、依赖声明、锁文件与已安装依赖树均已移除 Phosphor 图标库；移除后 `make check`、`make build` 再次通过。

依赖清理后，前端直接运行时依赖从 15 项降为 10 项；5 个移除项仍由 Lobe UI 间接依赖，锁文件包节点及版本不变。`make check`、`make build` 通过（前端 99 项通过，后端 88 项通过、2 项跳过）；产物分析与清理边界见 [依赖分析](dependencies.md)。

## 自动检查

| 范围 | 结果 |
| --- | --- |
| 后端 Ruff、Python 编译检查 | 通过 |
| 后端测试，真实本机 PostgreSQL / Redis | 90 项通过，含双实例子任务、数据库升级、编辑分支、附件与重启恢复 |
| 后端回归，SQLite / 测试事件流 | 88 项通过、2 项 PostgreSQL 专用测试跳过 |
| 前端 ESLint | 通过 |
| 前端 Vitest | 99 项通过，含正文边界、后置工具组、退出/重新展开、演示流程、子任务卡片、公共接入、分轮滚动与输入聚焦回归 |
| TypeScript / Vite 生产构建 | 通过 |
| Alembic migration 与模型一致性 | `alembic check` 无差异 |

后端覆盖 provider / 压缩 / 批处理、首次提交幂等性、消息版本、取消、租约 fence、Redis 发布失败后的 snapshot、提问跨重启恢复，以及附件真实解析、会话隔离、私有内容投影、不可覆盖写入和删除回收。
数据库测试使用专属随机 schema，结束后只清理自身 schema；未清空 Redis 或其他数据库。

根目录 `.env.example` 覆盖全部 Settings 字段，并验证环境变量优先级、空密钥、JSON 数组、S3 凭据传递及相对路径解析。切换工作目录不会读取其他目录的 `.env`；关闭了模型依赖默认的隐式 dotenv 加载。通过 `make dev` 指定后端 8017、前端 5187，真实 PG/Redis 启动成功，前端代理 API 返回 200，浏览器页面无脚本错误。`.env` 已被 Git 忽略，权限为 0600。

## 浏览器验证

主导航检查：首页、侧边栏示例与聊天分组、文字标题、宽度调整与折叠、中英文和深浅主题切换正常。侧栏使用主题对应的不透明背景，覆盖桌面展开、悬停预览和窄屏抽屉。语言切换保留消息草稿与用户会话标题；提问回答草稿和选项原文由组件测试验证。窄屏抽屉支持 Escape 关闭并恢复触发按钮焦点。

导航与聊天页顶部栏统一为 50px，可点击导航项统一为 32px。深色输入框使用独立面板背景、可见边框与聚焦高亮。空白新对话不显示会话操作按钮；已保存会话可通过详情页或侧栏菜单确认删除。

删除验证使用两条独立的合成会话，分别通过详情页、侧栏菜单完成确认删除。两条会话及其消息在 PostgreSQL 中均已清理，详情 API 返回 404；删除当前会话后跳转 `/chat`，删除其他会话时保留当前页面。

附件布局使用合成 PDF 和长图在浏览器检查：PDF 卡片按内容自动确定高度，图片保持正方形预览，托盘顶部对齐并保留 14px 内边距。辅助状态通过 `.sr-only` 向屏幕阅读器提供，不参与可见布局；添加文件按钮使用独立样式类，中英文具有相同的点击区域。

历史选择器的图标与标题保持同行，长标题单行省略并可悬停查看全文。公共选择器、菜单、错误间距与加载指示器的基础样式在前端项目内完整定义。

本次布局截图：[附件失败状态](screenshots/attachments.png)、[深色附件就绪状态](screenshots/attachments-dark.png)、[历史标题省略](screenshots/history.png)。附件使用合成测试内容，历史截图只展示启动连通性测试的标题。

使用独立测试 schema、真实 PostgreSQL / Redis 和显式测试模型，验证：

- `/chat` 首次发送后进入 `/chat/:id`；SSE、提问卡、Markdown 表格及代码块正常渲染。
- `ask_user` 等待时结束后端进程，重新启动并刷新，待回答问题仍在；回答后同一个 Run 继续完成。
- 重新生成、前后版本切换、刷新恢复历史，以及删除确认后返回空白对话。
- `/sidebar` 复用同一会话；侧边栏和深色主题正常显示。

截图使用明确标注的测试内容：[提问](screenshots/question.png)、[完整对话](screenshots/chat.png)、[侧边栏](screenshots/sidebar.png)、[深色侧边栏](screenshots/sidebar-dark.png)。

`tests/browser_app.py` 仅供显式浏览器测试，要求专用测试数据库 URL；`make dev` 和正式 FastAPI 入口不会使用测试模型。

使用正式 `app.main:create_app` 启动验证：健康检查、历史 API、构建后的 `/chat` 与 `/sidebar` 均返回 200；浏览器无脚本错误。缺少模型密钥时，Agent 明确显示不可用，`/readyz` 按预期返回 503。浏览器测试使用的独立 schema 在测试结束后清理。

## 验证边界

配置模型密钥后，已通过浏览器验证真实 DeepSeek `deepseek-v4-flash-vision-exp` 调用：启动测试消息收到“CornAgent 已正常启动。”，SSE 正常结束，刷新详情页后回复仍在。正式服务的 PostgreSQL、Redis 和 `/readyz` 均通过检查。该次验证覆盖模型连通性、流式回复和历史持久化；前述提问、恢复及文件边界的自动测试仍使用确定性测试模型。

本地文件存储经过真实读写测试。S3 适配器通过 SDK Stubber 验证条件写入、checksum、读取及删除请求，尚未连接实际 S3 兼容服务。

## 消息操作与公共接入验证

共享确认弹窗恢复 Ant Design 原生面板与布局，移除仅作用于描述区的卡片样式和半透明外壳。合成弹窗浏览器验证：深浅主题均为不透明背景、统一 16px 内边距，标题和页脚处于同一面板；删除按钮为红底白字，默认焦点在取消按钮。全量检查通过，前端 88 项、后端 87 项通过（2 项 PostgreSQL 专用测试跳过）；最终按钮调整后再次通过 ESLint、会话操作组件测试及构建。验证未删除实际会话，临时验证页面已清理。

使用独立临时会话和真实模型，在浏览器完成首次发送、编辑为第二版、切回第一版、再次选第二版及两次刷新；页面分别恢复对应用户消息、回答与 `1/2`、`2/2` 版本状态。临时会话及其四条消息已通过会话删除接口清理，详情返回 404。

用户操作栏预留 28px 高度，鼠标离开时隐藏复制、编辑按钮，悬停时显示而不改变布局；键盘聚焦与触屏同样可操作。完整聊天页和右侧栏使用同一组件。侧栏编辑草稿在语言切换后保留，取消不会发起编辑请求。

系统主题变化、手动主题优先级和 Ant Design 黑白主色由组件测试覆盖。浏览器检查浅色开启按钮为黑底白字，深色为白底黑字；编辑器主按钮与同一主题保持一致。示例页已移除跳转到完整聊天页的链接。

滚动到底部按钮按下时，实测中心横纵位移均为 0px，使用 `scale: 0.96` 提供点击反馈。消息区保留最小水平留白。

截图：[用户消息版本](screenshots/message-versions.png)、[侧栏消息操作](screenshots/sidebar-versions.png)、[黑白主题](screenshots/monochrome-dark.png)。消息内容均来自显式临时验证，未包含用户实际对话正文。

公共 Provider 测试覆盖开启、关闭和主题/语言变化时工作区不重载；HTTP transport 测试覆盖自定义 API 路径的 JSON、SSE 和附件地址。接入示例见 [前端接入](frontend-integration.md)。

## 示例页面与紧凑编辑

示例页标题为“在您的业务页面，询问AI”，下方为 Ant Design Skeleton 组合的作者、封面和段落占位布局，移除原描述和卡片；顶部按钮为“询问AI / Ask AI”。骨架组件独立放在 `ArticleSkeleton.tsx`。

浏览器通过合成 UI 验证单行编辑：输入使用 `INPUT`，内边框为 `0px`、内阴影为 `none`，输入与两枚按钮中心在同一水平线上。340px 窄屏英文界面中，编辑器宽度与滚动宽度均为 310px，无横向溢出。版本切换位于复制、编辑按钮组右侧。编辑失败后的 Enter 重试由组件测试覆盖。

聊天输入文字统一为 14px；深浅主题聚焦均使用蓝色 `#266df0` 边框/阴影。此轮样式验证未修改或提交实际用户会话，临时 UI 验证文件已移除。

## 每轮对话与过程展示

2026-09-06：渲染演示控制条改为单行，播放/暂停/继续共用按钮；重置、语言、主题使用图标，专项演示使用纯文字，移除宽窄切换。中英文示例共用时间轴与内容 ID，切换语言保留进度、暂停及展开状态；英文思考、正文、工具标题和结果均使用英文示例。工具组场景验证前 3 个平铺，第 4 个自动收起，手动展开后第 5 个追加不会再次收起；运行中的英文工具也保持流光效果。

浏览器验证同一播放按钮切换图标、暂停期间正文与流光冻结、继续后从原进度推进、重置清除暂停。1129×994 视口中内容区在工具平铺与收起前后均高 728px，收起后的组内容高 0px；390×720 视口中英文内容区宽 366px，控制条可横向滚动，正文不被裁切，无页面横向溢出。浏览器使用合成 API 数据，无模型调用与脚本错误。`make check` 通过（前端 110 项，后端 88 项通过、2 项 PostgreSQL 专用测试跳过），`make build` 通过。

渲染页使用独立的满高布局，内容区直接展示 Markdown，与上方标题左对齐，无卡片边框、内边距或底色。915×994 视口中内容区高 708px，正文流式增长的 199 帧采样高度变化为 0px；空白等待、过程展开后也保持该高度。915×620 视口及 390px 窄栏中高 334px，长内容在区域内滚动，End 键滚动不会带动页面。390×720 手机视口同样左对齐且无横向溢出；窄栏仅改变宽度。返回聊天页后不保留渲染页布局类。`make check`、`make build` 通过，前端 99 项通过，后端 88 项通过、2 项跳过；浏览器无脚本错误。

内容预览入口已迁入左侧导航的 `/rendering` 路由，演示区域只挂载正式 `MarkdownMessageContent` 及演示控制按钮，移除底部辅助说明。工具运行标题使用正式流光样式，依次展示读取、核验和完成状态；浏览器确认运行标题使用 `ShimmerText` 双层动画 `text-shimmer`（2 秒周期、400% 高亮带），禁用按钮透明度为 `1`，没有完整聊天页、用户气泡或输入框。可单独播放工具、新正文或完整思考/工具/正文流程。

浏览器通过左侧导航从侧边栏示例进入渲染页，确认只有该项标记当前页面；深浅主题高亮分别使用亮色和深色，390px 窄栏无横向溢出。高亮层文字与显示标题一致，辅助技术只读取一次标题，流程完成后高亮层移除。生产静态路由回归覆盖 `/rendering` 及既有聊天/侧栏路由、资源加载和 API 404。该轮前端 88 项、后端 88 项测试通过（2 项 PostgreSQL 专用测试跳过），ESLint、Ruff 和前端构建通过。

工具标题与工具组标题接入共用 `TextSwap`。浏览器对工具运行转完成、工具组合并与汇总连续采样：旧文字向上移至约 -4px、模糊接近 2px 并淡出，新文字从正向位移恢复为清晰文字；每段约 150ms。深色 390px 窄栏下，中英文切换正常，标题仍单行省略且无横向溢出。组件测试覆盖顺序退出、连续更新只落到最新标题、辅助技术名称即时同步及减少动态效果时直接更新。演示增加 900ms 完成状态停留，便于观察单工具文字切换。该轮前端 91 项、后端 88 项测试通过（2 项 PostgreSQL 专用测试跳过），ESLint、Ruff 与构建通过。

首 token 演示从 1600ms 的空内容等待开始，正式渲染器显示无图标的“正在思考”。浏览器连续采样确认首个有效内容到达后，旧标题 50ms 上移、模糊淡出，新标题 50ms 从下方进入；分隔线以左侧为原点展开，标题高度保持约 34.6px。点击“已处理”前后位置、尺寸与 `transform: none` 保持一致；390px 窄栏无横向溢出，首页问候语不再带句号。组件测试覆盖思考/正文/工具首内容、空白占位、标题节点复用、正文增长与耗时更新不重播、独立思考内容的折叠条件；演示静态帧保持引用。该轮前端 96 项、后端 88 项测试通过（2 项 PostgreSQL 专用测试跳过），ESLint、Ruff 与 TypeScript/Vite 构建通过。

正文边界退出改为约 440 毫秒的高度收起与从左向右推进的上移裁切。浏览器连续采样观察到旧正文高度从约 134px 逐步归零，左侧裁切先到顶部、右侧随后收起；退出节点立即带 `inert`。组件测试覆盖退出内容快照及中途重新展开，演示脚本测试覆盖工具运行/完成切换与新正文首次出现。该轮前端 88 项测试、ESLint 与 TypeScript/Vite 构建通过；后端默认回归 87 项通过、2 项 PostgreSQL 专用测试跳过。预览只使用合成内容，不调用模型或修改实际会话。

最新一轮的用户消息与 AI 回答共用整屏区域；发送后从消息区顶部开始，短回复结束后仍保留空间。浏览器合成对话验证了输入框发送、连续追加、长回复增长、上滚暂停跟随、返回最新位置、加载历史与窗口缩放。939px 页面中新一轮距顶部约 32px，390px 页面及 420px 侧栏约 24px；加载历史后原消息位置偏差小于 1px。完整聊天页只读复查通过，无横向溢出或浏览器脚本错误。

仅有过程或空白正文时，“已处理”保持展开且无法收起；正文到达后才恢复折叠。组件测试同时覆盖正文移除后重新展开、已有正文手动展开状态以及新正文边界。消息分组测试覆盖分页补齐用户消息和编辑分支，滚动测试覆盖增长、阅读历史、容器尺寸和过期加载结果。

过程区按最后一个非空正文块的位置切分，只收入其上方内容；该正文之后的工具组、子任务、思考与问题按原顺序留在外部。空白正文占位不移动边界，新正文到达后才将此前内容收入过程区。新增回归覆盖前后工具分区、待回答问题、空白占位、工具组展开状态、新正文与历史快照恢复。浏览器用合成部件验证工具从单项追加成组、空白正文保留展开、新正文更新边界、历史重挂载与 390px 窄栏展示，未修改实际会话数据。此轮 `make check`、`make build` 通过，前端 84 项通过，后端默认回归 87 项通过、2 项 PostgreSQL 专用测试跳过。

同一会话新一轮采用 360 毫秒缓动滚动。939px 与 390px 浏览器逐帧采样均记录到 20 个不同滚动位置，方向连续、逐渐减速并准确到达目标；窄屏无横向溢出。新增测试验证动画期间流式尺寸变化不会瞬移、手动滚动可中断并显示返回按钮，以及系统减少动态效果时直接定位。

本轮 `make check` 与 `make build` 通过；浏览器布局验证使用合成 UI，未调用模型或修改用户会话。临时页面已清理。


## 子 Agent 迁移验证

本轮执行 `make check`、`make build`，并以独立随机 schema 运行完整真实 PostgreSQL/Redis 回归：后端 89 项通过；默认 SQLite 检查为 87 项通过、2 项 PostgreSQL 竞争测试跳过；前端 26 个测试文件、81 项通过。新旧 Alembic 安装路径分别验证全新 schema 和已有会话/消息/Run 升级。本机配置数据库已升级到 `0002_subagents`，`alembic check` 无模型差异。

确定性 Root / Child 模型覆盖五个编排工具、Root 继续搜索、三种 Child 角色、分批完整收取与最终回答。并发与恢复回归覆盖双实例抢占、每 Root 限额、共享实例仅一个名额时的等待释放、稳定调用幂等、完成序号、等待重启、过期 Child 租约重执行、截止时间、关闭新派发后继续恢复，以及 Root 取消/失败/删除后的迟到结果拒绝。

完成检查覆盖失败、needs_input、超时结果仍需收取，Child 在 Root 等待用户期间完成不代替用户回答，以及模型生成期间 Child 完成也不能直接提交未收取结果的候选回答。结果边界包括超过 64 KiB 的单个完整结果、8000 字符展示投影、checkpoint 超限回滚交付标记、压缩历史后完整交付、Root 投影空间不足仍保存 Child 结果，以及等待恢复时 provider 压缩失败的明确终态。

事件测试覆盖 Redis 发布失败、跨 epoch 和序号缺口先发权威快照，以及终态快照。前端测试覆盖 task_id 稳定更新而不重复卡片、等待状态保持活跃、任务状态更新、结果展开、主题对比色和模型标题不被语言切换翻译。原有 PDF/图片、ask_user、编辑/重新生成、历史和滚动测试保持通过。

浏览器使用 `tests/subagent_browser_app.py` 显式测试入口、独立 PostgreSQL schema 和真实 Redis，实际执行两次完整并行流程和一次等待中停止。验证聊天页、侧边栏、等待时刷新重连、中英文/深浅主题切换、任务摘要展开、Markdown 对比表和停止按钮；页面无脚本错误或横向溢出，同一组 3 个 task_id 恰好生成 3 张卡片。数据库核验 6 个任务 completed + delivered，3 个 cancelled + ignored，全部仅执行一次、无残留租约。

截图均为固定测试模型与本地模拟资料：[深色任务摘要](screenshots/subagents-dark.png)、[浅色英文状态](screenshots/subagents-light-en.png)、[侧边栏任务](screenshots/subagents-sidebar.png)、[等待状态](screenshots/subagents-waiting.png)、[取消状态](screenshots/subagents-cancelled.png)。测试进程和隔离 schema 在验证后清理，Redis 未被清空。

另外在正式 LiteLLM 适配器的请求边界验证 Child 只携带独立 JSON 提示词及只读工具目录，Root 提示词保持原样。

本轮子 Agent 路径使用确定性测试模型，没有调用真实模型服务；默认应用仍通过现有 LiteLLM 接口进行模型推理，仅 `mock_web_search` 使用固定虚构资料。新工具挂载方式、模型注入、实际模型演示提示及移植来源见 [子任务与工具扩展](subagents.md)。


## 子任务展示简化

按浏览器标注移除子任务标题旁的状态标记，以及角色、必需标记、任务组、耗时和交付详情行；删除对应专用样式与闲置文案，保留图标、原标题和结果展开。聊天页与侧边栏复用同一组件。上方迁移截图记录简化前的界面。

`make check`、`make build` 通过，前端仍为 81 项通过。对标注页面只读验证：3 个子任务标题均保留，状态标记 0 个、详情行 0 行，浏览器无脚本错误。
